Add a public POST endpoint at /api/register-install to this Lovable project. It accepts JSON with these fields: engine (text), version (text), site_url (text). Insert a row into a Supabase table called installs with these fields plus id (uuid, primary key, default gen_random_uuid()), created_at (timestamptz, default now()). Return 200 OK with { success: true }. No authentication required. Also create an /admin/installs page showing all installs ordered by created_at descending. Columns: site URL as a clickable link, engine badge colour-coded by category, version, installed date. Show total install count at the top. Show a bar chart using recharts of installs per engine. Add a Lazy Admin link to /admin/installs in the main admin sidebar.
